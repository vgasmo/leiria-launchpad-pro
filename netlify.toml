# Netlify configuration for SPA routing
# All unknown routes fall back to index.html for React Router

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
